<?php

namespace App\Services;

use App\Data\{{modelName}}Data;
use App\Data\{{modelName}}FilterData;
use App\Exceptions\BadRequestException;
use App\Models\{{modelName}};
use App\Repositories\{{modelName}}Repository;
use Illuminate\Pagination\LengthAwarePaginator;

class {{modelName}}Service
{

    public function __construct(
        private readonly {{modelName}}Repository ${{modelNameCamelCase}}Repository
    )
    {
    }

    /**
     * Create {{modelNameSpaceCase}}.
     *
     * @param {{modelName}}Data ${{modelNameCamelCase}}Data
     *
     * @return {{modelName}}|null
     * @throws BadRequestException
     */
    public function create({{modelName}}Data ${{modelNameCamelCase}}Data): ?{{modelName}}
    {
        ${{modelNameCamelCase}} = $this->{{modelNameCamelCase}}Repository->save(${{modelNameCamelCase}}Data);

        if (empty(${{modelNameCamelCase}})) {
            throw new BadRequestException('Failed to create {{modelNameSpaceCase}}.');
        }

        return ${{modelNameCamelCase}};
    }

    /**
     * Get paginated {{modelNameSpaceCasePlural}}.
     *
     * @param {{modelName}}FilterData ${{modelNameCamelCase}}FilterData
     *
     * @return LengthAwarePaginator<{{modelName}}>
     */
    public function getPaginated({{modelName}}FilterData ${{modelNameCamelCase}}FilterData): LengthAwarePaginator
    {
        return $this->{{modelNameCamelCase}}Repository->getPaginated(${{modelNameCamelCase}}FilterData);
    }

    /**
     * Get {{modelNameSpaceCase}} by id.
     *
     * @param int $id
     * @param array $relations
     *
     * @return {{modelName}}|null
     * @throws BadRequestException
     */
    public function getById(int $id, array $relations = []): ?{{modelName}}
    {
        ${{modelNameCamelCase}} = $this->{{modelNameCamelCase}}Repository->findById($id, $relations);

        if (empty(${{modelNameCamelCase}})) {
            throw new BadRequestException('{{modelNameUpperFirstSpaceCase}} not found.');
        }

        return ${{modelNameCamelCase}};
    }

    /**
     * Update {{modelNameSpaceCase}}.
     *
     * @param {{modelName}}Data ${{modelNameCamelCase}}Data
     *
     * @return {{modelName}}|null
     * @throws BadRequestException
     */
    public function update({{modelName}}Data ${{modelNameCamelCase}}Data): ?{{modelName}}
    {
        ${{modelNameCamelCase}} = $this->{{modelNameCamelCase}}Repository->findById(${{modelNameCamelCase}}Data->id);

        if (empty(${{modelNameCamelCase}})) {
            throw new BadRequestException('Failed to update {{modelNameSpaceCase}}.');
        }

        ${{modelNameCamelCase}} = $this->{{modelNameCamelCase}}Repository->save(${{modelNameCamelCase}}Data, ${{modelNameCamelCase}});

        if (empty(${{modelNameCamelCase}})) {
            throw new BadRequestException('Failed to update {{modelNameSpaceCase}}.');
        }

        return ${{modelNameCamelCase}};
    }

    /**
     * Delete {{modelNameSpaceCase}}.
     *
     * @param int $id
     *
     * @return bool
     * @throws BadRequestException
     */
    public function delete(int $id): bool
    {
        $isDeleted = $this->{{modelNameCamelCase}}Repository->delete($id);

        if (!$isDeleted) {
            throw new BadRequestException('Failed to delete {{modelNameSpaceCase}}.');
        }

        return true;
    }

}
